int x, y;
std::cout << "enter x, y: ";
std::cin >> x >> y;
int r, R;
std::cout << "enter r, R: ";
std::cin >> r >> R;
if (sqrt(pow(x, 2) + pow(y, 2)) <= pow(R, 2) and sqrt(pow(x, 2) + pow(y, 2)) >= pow(r, 2))
std::cout << "Mur <3\n";
else
std::cout << "bob\n";
double x, y;
std::cout <<"Enter x, y: ";
std::cin >> x >> y;
double r, R;
std::cout << "enter r, R: ";
std::cin >> r >> R;
if (sqrt(pow(x, 2) + pow(y, 2)) <= pow(R, 2) and sqrt(pow(x, 2) + pow(y, 2))>= pow(r, 2))
std::cout << "Mur <3\n";
else
std::cout << "bob\n";
